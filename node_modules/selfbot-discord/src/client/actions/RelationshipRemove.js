const Action = require('./Action');

class RelationshipRemoveAction extends Action {
  handle(data) {
    const relation = this.client.dataManager.killRelation(data);
    return { relation };
  }
}

module.exports = RelationshipRemoveAction;
